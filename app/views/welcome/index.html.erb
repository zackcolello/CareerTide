<!DOCTYPE html>
<html>
<h1>Welcome#index</h1>
<p>Find me here</p>

<%= "Count to 10" %>
<br />
<% 11.times do |i| %>
    <%= i %>
<%end%>

<% if(params.has_key?(:id) &&
params.has_key?(:random)) %><br/>
    <%= "ID :  #{params[:id]}" %> <br />
    <%= "ID :  #{params[:random]}" %> <br />

<%end%>
</html>
